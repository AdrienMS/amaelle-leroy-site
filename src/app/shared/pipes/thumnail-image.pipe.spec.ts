import { ThumbnailImagePipe } from './thumbnail-image.pipe';

describe('ThumnailImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbnailImagePipe();
    expect(pipe).toBeTruthy();
  });
});
