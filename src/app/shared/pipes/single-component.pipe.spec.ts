import { SingleComponentPipe } from './single-component.pipe';

describe('SingleComponentPipe', () => {
  it('create an instance', () => {
    const pipe = new SingleComponentPipe();
    expect(pipe).toBeTruthy();
  });
});
