import { SingleGalleryPipe } from './single-gallery.pipe';

describe('SingleGalleryPipe', () => {
  it('create an instance', () => {
    const pipe = new SingleGalleryPipe();
    expect(pipe).toBeTruthy();
  });
});
