import { SinglePagePipe } from './single-page.pipe';

describe('SinglePagePipe', () => {
  it('create an instance', () => {
    const pipe = new SinglePagePipe();
    expect(pipe).toBeTruthy();
  });
});
